/**
 * @file BuildConfig.cpp
 *
 */

#include "BuildConfig.hpp"

namespace settings {

const char* windowTitle = "PolyMorph TD";

const TimeDuration uiSystemSync = TimeDuration::millis(33);
const TimeDuration grSystemSync = TimeDuration::millis(16);

}
